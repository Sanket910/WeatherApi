import { AutoSuggestConstants } from './auto-suggest-constants';

describe('AutoSuggestConstants', () => {
  it('should create an instance', () => {
    expect(new AutoSuggestConstants()).toBeTruthy();
  });
});
